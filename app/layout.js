export const metadata = {
  title: 'Xbox One Controller SVG',
  description: 'Detailed SVG illustration of an Xbox One controller with accurate proportions',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  )
}
